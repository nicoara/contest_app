<%= form_with(model: division, local: true) do |form| %>
  <% if division.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(division.errors.count, "error") %> prohibited this division from being saved:</h2>

      <ul>
      <% division.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :division_name %>
  </div>

  <div class="field">
    <%= form.label :ranking %>
    <%= form.text_field :ranking, id: :division_ranking %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
