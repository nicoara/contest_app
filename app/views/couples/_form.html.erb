<%= form_with(model: couple) do |f| %>

  <%= render partial: 'select_dancer', locals: {f: f, field: :boy_id, list: @boys, selected_id: @couple.boy_id} %>
  <%= render partial: 'select_dancer', locals: {f: f, field: :girl_id, list: @girls, selected_id: @couple.girl_id} %>

<%= f.submit "Submit", class: "btn btn-primary" %>
<% end %>
