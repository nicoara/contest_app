<%= form_with(model: participant, local: true) do |form| %>
  <% if participant.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(participant.errors.count, "error") %> prohibited this participant from being saved:</h2>

      <ul>
      <% participant.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :couple_id %>
    <%= form.number_field :couple_id, id: :participant_couple_id %>
  </div>

  <div class="field">
    <%= form.label :competition_id %>
    <%= form.number_field :competition_id, id: :participant_competition_id %>
  </div>

  <div class="field">
    <%= form.label :division_id %>
    <%= form.number_field :division_id, id: :participant_division_id %>
  </div>

  <div class="field">
    <%= form.label :agesection_id %>
    <%= form.number_field :agesection_id, id: :participant_agesection_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
