<%= form_with(model: age_section, local: true) do |form| %>
  <% if age_section.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(age_section.errors.count, "error") %> prohibited this age_section from being saved:</h2>

      <ul>
      <% age_section.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :age_section_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
